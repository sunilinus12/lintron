{
  "name": "lintron-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "start:desktop": "electron apps/desktop",
    "start": "electron apps/desktop",
    "build:lintron": "npm run build --workspace=packages/lintron",
    "test": "echo \"Lintron Monorepo Ready\" && exit 0"
  },
  "dependencies": {
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "electron": "^31.7.7"
  }
}
